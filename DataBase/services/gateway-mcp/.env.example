DATABASE_GATEWAY_URL=https://database.tengokukk.com

# Optional. Required only when the Gateway is configured with
# DATABASE_GATEWAY_AUTH_REQUIRED=true.
DATABASE_GATEWAY_API_KEY=replace-with-generated-key

# Default actor used by write tools when caller does not provide one.
DATABASE_GATEWAY_MCP_ACTOR=database-gateway-mcp
