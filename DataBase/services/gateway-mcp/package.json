{
  "name": "@emptyinkpot/database-gateway-mcp",
  "version": "0.1.0",
  "description": "MCP adapter for the DataBase Gateway HTTP API",
  "private": true,
  "type": "module",
  "scripts": {
    "mcp": "node ./mcp/server.mjs",
    "smoke": "node --check ./mcp/server.mjs && node ./scripts/smoke.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "zod": "^4.0.0"
  },
  "engines": {
    "node": ">=22"
  }
}
