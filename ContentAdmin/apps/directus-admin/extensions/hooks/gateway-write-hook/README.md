# gateway-write-hook

Planned Directus hook for Gateway-mediated writes.

The hook should validate that canonical content mutations call DataBase Gateway
or ContentBase Runtime endpoints instead of updating canonical tables directly.
