{
  "name": "@emptyinkpot/content-admin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Independent content administration and AI writing workbench for DataBase and ContentBase SDKs.",
  "scripts": {
    "typecheck": "pnpm -r --if-present typecheck",
    "build": "pnpm -r --if-present build",
    "lint": "pnpm -r --if-present lint"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "packageManager": "pnpm@10.16.1"
}
